package tests.base.login;

import constants.HomePageLocators;
import helpers.WaitFor;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.testng.Assert;
import org.testng.annotations.Test;
import tests.base.BaseTest;

public class Footer extends BaseTest {
//    @Test (groups = {"home url","footer"})
//    public void footerUrl(){
//        driver.get(HomePageLocators.HOME_URL);
//
//        WaitFor.WaitFori(driver, HomePageLocators.ABOUT_FOOTER);
//        driver.findElement(By.cssSelector(HomePageLocators.ABOUT_FOOTER)).click();
//        Assert.assertEquals(driver.getCurrentUrl(), HomePageLocators.ABOUT_URL);
//    }

}
